### Elephant APT Packages

Find and manage packages using APT.

#### Features

- Search for packages in configured repositories
- Install/Remove single packages
- List all packages or installed packages

#### Requirements

- `apt` package manager (tested with apt and Ubuntu 24.04)
